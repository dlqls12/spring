<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sbs.lyb.at.dao.ArticleDao">
	<select id="getForPrintArticles" parameterType="map" resultType="Article">
		SELECT *
		FROM article
		WHERE displayStatus = 1
		ORDER BY id DESC
	</select>

	<select id="getForPrintArticleById" parameterType="int" resultType="Article">
		SELECT *
		FROM article
		WHERE id = #{id}
	</select>

	<insert id="write" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO article
		SET regDate = NOW(),
		updateDate = NOW(),
		displayStatus = 1,
		title = #{title},
		body = #{body}
	</insert>

	<update id="delete" parameterType="int">
		UPDATE article
		SET delStatus =
		1,
		delDate = NOW(),
		displayStatus = 0
		WHERE id = #{id}
	</update>

	<update id="modify" parameterType="map">
		UPDATE article
		SET title =
		#{title},
		body = #{body}
		WHERE id = #{id}
	</update>

	<select id="getArticles" parameterType="map" resultType="Article">
		SELECT *
		FROM article
		WHERE displayStatus = 1
		ORDER BY id DESC
		LIMIT ${limitFrom}, 10;
	</select>
	
	<select id="getForPrintArticleReplies" parameterType="int" resultType="ArticleReply">
		SELECT *
		FROM articleReply
		WHERE articleId = ${id}
		ORDER BY id DESC
	</select>
	
	<insert id="writeReply" parameterType="map" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO articleReply
		SET regDate = NOW(),
		updateDate = NOW(),
		body = #{body},
		articleId = #{articleId}
	</insert>
</mapper>